# Variables pour Vercel / vercel dev (api/config.js et fonctions serverless)
# Copiez en .env.local pour le dev local : cp .env.example .env.local

SUPABASE_URL=https://VOTRE-PROJECT-ID.supabase.co
SUPABASE_ANON_KEY=VOTRE-CLE-ANON-PUBLIQUE

# Cote serveur uniquement (login API, pedidos, emails…) — ne jamais exposer au navigateur
SUPABASE_SERVICE_ROLE_KEY=VOTRE-CLE-SERVICE-ROLE
