{
  "name": "scan-client-mobile",
  "version": "1.0.0",
  "description": "Frontend y API serverless (Vercel) para BATMAR",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start:local": "node scripts/dev-server.js",
    "serve:static": "npx serve . -p 3000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "nodemailer": "^6.10.1",
    "pdfkit": "^0.18.0"
  }
}
